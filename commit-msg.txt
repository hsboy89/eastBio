Fix: 회사소개 상단 방식으로 card 재조회 완전 차단

- variants + animate 방식 제거
- 각 card에 직접 whileInView 적용 (회사소개 상단과 동일한 방식)
- viewport={{ once: true }}로 한 번만 실행 보장
- useEffect + IntersectionObserver 제거
- 모든 grid를 일반 div로 변경 (motion.div 제거)
- BusinessArea, Strengths, PartnersNetwork, Contact에 적용
